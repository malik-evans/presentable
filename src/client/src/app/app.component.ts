import {Component} from '@angular/core';

@Component({
  selector: 'app-root',
    template: `
        <div class="ui container">
            <h1>Hello</h1>
        </div>
    `
})
export class AppComponent {
    title = 'app works';
}
